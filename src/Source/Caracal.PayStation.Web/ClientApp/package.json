{
  "name": "pay-station",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --no-open --serve --port 3336",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.5.2",
    "@stencil/sass": "^1.4.1"
  },
  "license": "MIT",
  "dependencies": {
    "caracal_apex": "^2.1.3",
    "caracal_polaris": "^2.0.3",
    "chart.js": "^3.1.1"
  }
}
